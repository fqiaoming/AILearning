![评估流程](./images/evaluation.svg)
*图：提示词测试评估的系统化流程*

# 第13课：提示词测试与评估方法

> 📚 **课程信息**
> - 所属模块：第一模块 - AI基础与环境搭建  
> - 章节：第3章 - 提示词工程精通（第6/8课）
> - 学习目标：掌握提示词的测试、评估和优化方法，建立科学的工作流程
> - 预计时间：60-70分钟
> - 前置知识：第08-12课

---

## 📢 课程导入

### 前言

你精心设计了一个提示词，测试了一次效果不错，就直接上线了。结果用户用起来问题百出！为什么？因为你只测试了一次，没有系统评估！

很多人写提示词就像盲人摸象，改一个地方测一下，感觉好像变好了，其实可能只是碰巧。真正的专业做法是：建立测试集、设计评估指标、系统化优化、A/B测试验证。这才是工程化的提示词开发！

---

### 核心价值点

**第一，提示词测试是工程化的关键环节。**

很多人以为写完提示词就完事了，其实这只是开始！专业的AI工程师会花50%的时间在测试和优化上。为什么？因为提示词的效果很不稳定，同样的提示词，不同的输入可能效果完全不同！

就像你写代码要写测试用例一样，提示词也需要测试集！没有测试，你永远不知道提示词在边界情况下会不会翻车。这就是专业和业余的区别！

**第二，科学的评估方法能让优化效率提升十倍。**

没有评估指标，你怎么知道改进后是变好了还是变差了？凭感觉？那太不靠谱了！专业的做法是：
- 定义明确的评估指标（准确率、召回率、F1分数）
- 建立标注数据集（至少50-100个测试样本）
- 每次修改后都跑一遍测试
- 用数据说话，而不是靠感觉

有了这套流程，你的优化方向会非常明确，效率提升不是一点点！

**第三，A/B测试是验证效果的金标准。**

你觉得新版本提示词更好，但用户不一定这么觉得！唯一的验证方法就是A/B测试：让50%用户用旧版，50%用新版，对比真实数据。

很多大公司的AI产品都是这样迭代的：每周上线5-10个提示词优化，通过A/B测试选出最好的。这就是为什么大公司的AI产品越来越好用，而小公司却止步不前！

**第四，系统化的测试流程是可以复用的。**

今天你学会这套方法，以后每个提示词都能用！建立测试集、定义指标、自动化测试、A/B验证，这套流程一旦建立，就能反复使用。

而且这个能力在面试中特别加分！很多公司面试AI工程师，都会问"你如何评估和优化提示词"。如果你能系统地讲出这套方法，面试官会刮目相看！

---

### 行动号召

今天这一课会教你：
- 如何构建提示词测试集
- 常用的评估指标和计算方法
- 系统化的优化流程
- A/B测试的设计和分析
- 自动化测试工具的使用

**学完这课，你的提示词工程将达到工业级水平！**

---

## 📖 知识讲解

### 1. 为什么需要系统化测试？

#
![Input Output](./images/input_output.svg)
*图：Input Output*

### 1.1 提示词的不确定性

```
同一个提示词，不同输入可能效果差异巨大：

示例：情感分类提示词

测试1：
输入："这个产品太棒了！"
输出：正面 ✓（正确）

测试2：
输入："这价格倒是不贵，可惜质量不行"
输出：正面 ✗（错误！应该是负面）

原因：
- 提示词对简单情况work，复杂情况fail
- 只测试一次根本发现不了问题
```

#### 1.2 真实场景的复杂性

```
生产环境的输入千奇百怪：

理想情况：
"这个手机很好用"

实际情况：
- "这手机还行吧，就是贵了点"（混合情感）
- "不能说好，也不能说差"（模棱两可）
- "emmm...怎么说呢"（表达不清）
- "哈哈哈哈哈"（纯表情）
- ""（空输入）

不测试这些边界情况，上线必翻车！
```

---

### 2. 构建测试数据集

#### 2.1 测试集的作用

```
测试集 = 一组精心准备的输入输出对

作用：
✅ 验证提示词的效果
✅ 对比不同版本的提示词
✅ 发现边界情况的问题
✅ 评估改进的效果
```

#### 2.2 测试集设计原则

**原则1：覆盖典型场景**

```
场景1：简单明确的情况（30%）
例："这个产品超级好" → 正面

场景2：中等复杂度（40%）
例："价格合理，但功能一般" → 中性

场景3：边界和困难情况（30%）
例："除了差，没别的词能形容" → 负面
```

**原则2：包含失败案例**

```
专门收集容易出错的样本：
- 反讽："这产品真是'好'啊"（负面）
- 双重否定："不是不好"（正面）
- 混合情感："好是好，就是贵"（中性偏正）
```

**原则3：数量要充足**

```
推荐数量：
- 最小可用：30-50个
- 推荐规模：100-200个
- 大规模：500-1000个

分配：
- 训练样本（Few-shot用）：20%
- 测试样本（评估用）：80%
```

---

#### 2.3 测试集示例

**情感分类测试集：**

```json
[
  {
    "id": 1,
    "input": "这个产品真的太好用了，强烈推荐！",
    "expected": "正面",
    "difficulty": "easy",
    "category": "明确正面"
  },
  {
    "id": 2,
    "input": "质量不行，退货了",
    "expected": "负面",
    "difficulty": "easy",
    "category": "明确负面"
  },
  {
    "id": 3,
    "input": "还可以吧，没什么特别的",
    "expected": "中性",
    "difficulty": "easy",
    "category": "明确中性"
  },
  {
    "id": 4,
    "input": "价格实惠，但功能一般，看个人需求",
    "expected": "中性",
    "difficulty": "medium",
    "category": "混合情感"
  },
  {
    "id": 5,
    "input": "这产品真是'好'啊，买了就后悔",
    "expected": "负面",
    "difficulty": "hard",
    "category": "反讽"
  },
  {
    "id": 6,
    "input": "emmm...怎么说呢",
    "expected": "中性",
    "difficulty": "hard",
    "category": "表达不清"
  }
]
```

---

### 3. 评估指标

#### 3.1 分类任务指标

**准确率（Accuracy）**

```
定义：预测正确的样本数 / 总样本数

计算：
正确：80个
总数：100个
准确率 = 80/100 = 80%

优点：简单直观
缺点：类别不平衡时不准确

示例：
如果99%的样本都是正面，模型全预测正面，
准确率99%，但实际没用！
```

**精确率（Precision）**

```
定义：预测为正的样本中，真正为正的比例

计算公式：
Precision = TP / (TP + FP)

TP（True Positive）：预测正，实际正
FP（False Positive）：预测正，实际负

例子：
预测为正面的有20个，其中18个真是正面
Precision = 18/20 = 90%

意义：
"我说是正的，有多大概率是对的"
```

**召回率（Recall）**

```
定义：真正为正的样本中，被预测为正的比例

计算公式：
Recall = TP / (TP + FN)

FN（False Negative）：预测负，实际正

例子：
实际正面的有25个，预测出18个
Recall = 18/25 = 72%

意义：
"实际是正的，我能找出多少"
```

**F1分数**

```
定义：精确率和召回率的调和平均

计算公式：
F1 = 2 × (Precision × Recall) / (Precision + Recall)

例子：
Precision = 90%
Recall = 72%
F1 = 2 × (0.9 × 0.72) / (0.9 + 0.72) = 79.6%

优点：
平衡precision和recall
类别不平衡时比准确率更有意义
```

---

#### 3.2 生成任务指标

**主观评分**

```
定义：人工评估生成质量

评分维度：
1. 准确性（是否正确）：1-5分
2. 相关性（是否相关）：1-5分
3. 流畅性（是否通顺）：1-5分
4. 完整性（是否完整）：1-5分

总分：20分满分

评分标准要明确，减少主观性
```

**ROUGE分数**

```
定义：对比生成文本和参考文本的重叠度

ROUGE-1：单词级别重叠
ROUGE-2：双词级别重叠
ROUGE-L：最长公共子序列

适用：文本摘要、翻译等
```

**BLEU分数**

```
定义：机器翻译质量评估

原理：对比译文和参考译文的n-gram重叠

分数范围：0-1（或0-100）
- 0.3+：可理解
- 0.5+：高质量
- 0.7+：极优秀

适用：翻译、代码生成等
```

---

### 4. 系统化优化流程

#### 4.1 标准优化流程

```
步骤1：建立基准（Baseline）
├─ 写初版提示词
├─ 在测试集上评估
└─ 记录初始指标（如准确率65%）

步骤2：问题分析
├─ 查看错误案例
├─ 分类错误类型
├─ 找出共同模式
└─ 确定优化方向

步骤3：针对性优化
├─ 根据问题修改提示词
├─ 可能的改进：
│   ├─ 增加Few-shot示例
│   ├─ 调整措辞
│   ├─ 增加约束条件
│   └─ 使用CoT
└─ 一次只改一处！

步骤4：效果验证
├─ 在测试集上重新评估
├─ 对比新旧指标
└─ 如果提升，保留；否则回退

步骤5：迭代优化
├─ 重复步骤2-4
├─ 直到达到目标指标
└─ 或者确定无法继续提升

步骤6：最终验证
├─ 在更大的数据集测试
├─ A/B测试
└─ 生产环境灰度发布
```

---

#### 4.2 错误分析方法

**分类错误案例**

```
错误类型1：边界情况处理不当
例子：
输入："emmm"
预测：正面
实际：中性
原因：提示词没有考虑模糊表达

优化：添加示例，教AI处理模糊情况

错误类型2：反讽识别失败
例子：
输入："这产品真是'太好'了（讽刺）"
预测：正面
实际：负面
原因：AI不理解反讽

优化：添加反讽示例，或用CoT分析语气

错误类型3：混合情感处理错误
例子：
输入："好是好，就是贵"
预测：正面
实际：中性
原因：AI只看到"好"，忽略了"贵"

优化：添加混合情感示例，强调要综合判断
```

---

### 5. A/B测试设计

#### 5.1 什么是A/B测试

```
A/B测试：
- 将用户随机分成两组
- A组使用旧版提示词
- B组使用新版提示词
- 对比两组的效果数据

目的：
用真实用户数据验证优化效果
```

#### 5.2 A/B测试流程

```
步骤1：确定假设
H0（原假设）：新版提示词效果不变或更差
H1（备择假设）：新版提示词效果更好

步骤2：设计实验
- 确定样本量（至少1000个样本）
- 确定测试时长（至少1周）
- 确定评估指标（如用户满意度）

步骤3：随机分组
- 50%用户 → A组（对照组，旧版）
- 50%用户 → B组（实验组，新版）
- 确保随机性

步骤4：收集数据
记录关键指标：
- 响应准确率
- 用户满意度评分
- 任务完成率
- 用户反馈

步骤5：统计分析
- 计算两组指标差异
- 进行显著性检验
- 判断差异是否有统计意义

步骤6：决策
如果新版显著更好：
- 全量上线新版
否则：
- 继续使用旧版或重新优化
```

---

## 💻 Demo案例：自动化测试系统

### 案例说明

构建一个自动化测试系统，评估不同提示词的效果。

### 代码实现

创建`prompt_testing_system.py`：

```python
"""
提示词自动化测试系统
支持多版本对比、自动评估、生成报告
"""

from openai import OpenAI
import os
from dotenv import load_dotenv
import json
from typing import List, Dict
from collections import Counter

load_dotenv()

client = OpenAI(
    base_url=os.getenv("LOCAL_LLM_BASE_URL"),
    api_key=os.getenv("LOCAL_LLM_API_KEY")
)


class PromptTester:
    """提示词测试器"""
    
    def __init__(self, test_data_file: str):
        """
        初始化测试器
        
        Args:
            test_data_file: 测试数据文件路径
        """
        with open(test_data_file, 'r', encoding='utf-8') as f:
            self.test_data = json.load(f)
        self.results = []
    
    def test_prompt(self, prompt_template: str, name: str = "unnamed") -> Dict:
        """
        测试一个提示词
        
        Args:
            prompt_template: 提示词模板，用{input}占位
            name: 提示词版本名称
            
        Returns:
            测试结果字典
        """
        print(f"\n{'='*60}")
        print(f"测试提示词版本：{name}")
        print(f"{'='*60}")
        
        correct = 0
        predictions = []
        errors = []
        
        for i, item in enumerate(self.test_data, 1):
            # 构造完整提示词
            prompt = prompt_template.format(input=item['input'])
            
            # 调用AI
            try:
                response = client.chat.completions.create(
                    model=os.getenv("LOCAL_LLM_MODEL"),
                    messages=[{"role": "user", "content": prompt}],
                    temperature=0.1,
                    max_tokens=100
                )
                prediction = response.choices[0].message.content.strip()
                
                # 提取预测结果（简单处理）
                if "正面" in prediction:
                    prediction = "正面"
                elif "负面" in prediction:
                    prediction = "负面"
                else:
                    prediction = "中性"
                
                predictions.append(prediction)
                
                # 判断是否正确
                is_correct = prediction == item['expected']
                if is_correct:
                    correct += 1
                else:
                    errors.append({
                        'id': item['id'],
                        'input': item['input'],
                        'expected': item['expected'],
                        'predicted': prediction,
                        'difficulty': item.get('difficulty', 'unknown')
                    })
                
                # 打印进度
                status = "✓" if is_correct else "✗"
                print(f"[{i}/{len(self.test_data)}] {status} "
                      f"输入：{item['input'][:30]}... "
                      f"预测：{prediction} "
                      f"实际：{item['expected']}")
                
            except Exception as e:
                print(f"[{i}/{len(self.test_data)}] 错误：{e}")
                predictions.append("ERROR")
                errors.append({
                    'id': item['id'],
                    'input': item['input'],
                    'error': str(e)
                })
        
        # 计算指标
        accuracy = correct / len(self.test_data)
        
        # 计算混淆矩阵（简化版）
        true_labels = [item['expected'] for item in self.test_data]
        confusion = self._compute_confusion_matrix(true_labels, predictions)
        
        # 计算F1分数（针对"正面"类）
        precision, recall, f1 = self._compute_f1(true_labels, predictions, "正面")
        
        result = {
            'name': name,
            'accuracy': accuracy,
            'correct': correct,
            'total': len(self.test_data),
            'errors': errors,
            'confusion_matrix': confusion,
            'precision': precision,
            'recall': recall,
            'f1': f1
        }
        
        self.results.append(result)
        return result
    
    def _compute_confusion_matrix(self, true_labels: List[str], 
                                  predictions: List[str]) -> Dict:
        """计算混淆矩阵"""
        matrix = {}
        for true, pred in zip(true_labels, predictions):
            key = f"{true}->{pred}"
            matrix[key] = matrix.get(key, 0) + 1
        return matrix
    
    def _compute_f1(self, true_labels: List[str], predictions: List[str],
                   target_class: str) -> tuple:
        """计算指定类别的Precision、Recall、F1"""
        tp = sum(1 for t, p in zip(true_labels, predictions)
                if t == target_class and p == target_class)
        fp = sum(1 for t, p in zip(true_labels, predictions)
                if t != target_class and p == target_class)
        fn = sum(1 for t, p in zip(true_labels, predictions)
                if t == target_class and p != target_class)
        
        precision = tp / (tp + fp) if (tp + fp) > 0 else 0
        recall = tp / (tp + fn) if (tp + fn) > 0 else 0
        f1 = 2 * precision * recall / (precision + recall) if (precision + recall) > 0 else 0
        
        return precision, recall, f1
    
    def compare_results(self):
        """对比多个版本的结果"""
        if len(self.results) < 2:
            print("需要至少2个测试结果才能对比")
            return
        
        print(f"\n{'='*60}")
        print("多版本对比结果")
        print(f"{'='*60}\n")
        
        # 表头
        print(f"{'版本':<15} {'准确率':<10} {'F1分数':<10} {'改进':<10}")
        print("-" * 60)
        
        # 基准
        baseline = self.results[0]
        print(f"{baseline['name']:<15} "
              f"{baseline['accuracy']*100:>7.2f}%  "
              f"{baseline['f1']*100:>7.2f}%  "
              f"{'(基准)':<10}")
        
        # 其他版本
        for result in self.results[1:]:
            improvement = (result['accuracy'] - baseline['accuracy']) * 100
            improvement_str = f"+{improvement:.2f}%" if improvement > 0 else f"{improvement:.2f}%"
            print(f"{result['name']:<15} "
                  f"{result['accuracy']*100:>7.2f}%  "
                  f"{result['f1']*100:>7.2f}%  "
                  f"{improvement_str:<10}")
    
    def analyze_errors(self, result_name: str = None):
        """分析错误案例"""
        result = self.results[-1] if result_name is None else \
                 next((r for r in self.results if r['name'] == result_name), None)
        
        if not result:
            print("找不到指定结果")
            return
        
        print(f"\n{'='*60}")
        print(f"错误分析：{result['name']}")
        print(f"{'='*60}\n")
        
        errors = result['errors']
        if not errors:
            print("没有错误案例！")
            return
        
        # 按难度分组
        by_difficulty = {}
        for error in errors:
            diff = error.get('difficulty', 'unknown')
            if diff not in by_difficulty:
                by_difficulty[diff] = []
            by_difficulty[diff].append(error)
        
        # 输出分析
        print(f"错误总数：{len(errors)}/{result['total']}")
        print(f"\n按难度分布：")
        for diff, errs in by_difficulty.items():
            print(f"  {diff}: {len(errs)}个")
        
        # 展示典型错误
        print(f"\n典型错误案例：")
        for i, error in enumerate(errors[:5], 1):
            print(f"\n案例{i}：")
            print(f"  输入：{error['input']}")
            print(f"  预期：{error['expected']}")
            print(f"  预测：{error['predicted']}")
            if 'difficulty' in error:
                print(f"  难度：{error['difficulty']}")


def create_test_data():
    """创建测试数据集"""
    test_data = [
        {
            "id": 1,
            "input": "这个产品真的太好用了，强烈推荐！",
            "expected": "正面",
            "difficulty": "easy"
        },
        {
            "id": 2,
            "input": "质量太差了，完全不值这个价",
            "expected": "负面",
            "difficulty": "easy"
        },
        {
            "id": 3,
            "input": "还可以吧，没什么特别的",
            "expected": "中性",
            "difficulty": "easy"
        },
        {
            "id": 4,
            "input": "价格实惠，但功能一般",
            "expected": "中性",
            "difficulty": "medium"
        },
        {
            "id": 5,
            "input": "这产品真是'好'啊，买了就后悔",
            "expected": "负面",
            "difficulty": "hard"
        },
        {
            "id": 6,
            "input": "外观设计很漂亮，但是用起来有点卡，总体还算可以",
            "expected": "中性",
            "difficulty": "medium"
        }
    ]
    
    with open('test_data.json', 'w', encoding='utf-8') as f:
        json.dump(test_data, f, ensure_ascii=False, indent=2)
    
    print("✓ 测试数据集已创建：test_data.json")


def main():
    """主函数"""
    print("🎯 提示词自动化测试系统")
    print("="*60)
    
    # 创建测试数据
    create_test_data()
    
    # 初始化测试器
    tester = PromptTester('test_data.json')
    
    # 版本1：基础版本（Zero-shot）
    prompt_v1 = """判断以下评论的情感倾向（正面/负面/中性）：

评论：{input}

情感："""
    
    result_v1 = tester.test_prompt(prompt_v1, name="V1-Zero-shot")
    
    # 版本2：Few-shot版本
    prompt_v2 = """判断评论的情感倾向（正面/负面/中性）

示例1：
评论：这个真的超级棒！
情感：正面

示例2：
评论：太失望了，不推荐
情感：负面

示例3：
评论：还行吧，一般般
情感：中性

---
评论：{input}
情感："""
    
    result_v2 = tester.test_prompt(prompt_v2, name="V2-Few-shot")
    
    # 版本3：Few-shot + 明确指导
    prompt_v3 = """判断评论的情感倾向（正面/负面/中性）

要点：
- 综合考虑所有内容，不要只看单个词
- 注意反讽和语气
- 混合情感倾向中性

示例1：
评论：这个真的超级棒！
情感：正面

示例2：
评论：太失望了，不推荐
情感：负面

示例3：
评论：还行吧，一般般
情感：中性

示例4：
评论：好是好，就是贵
情感：中性

---
评论：{input}
情感："""
    
    result_v3 = tester.test_prompt(prompt_v3, name="V3-优化版")
    
    # 对比结果
    tester.compare_results()
    
    # 分析最新版本的错误
    tester.analyze_errors()
    
    print("\n" + "="*60)
    print("✅ 测试完成！")
    print("\n💡 总结：")
    print("1. 建立测试集是系统化优化的基础")
    print("2. 定量指标让优化方向更明确")
    print("3. 错误分析帮助发现改进空间")
    print("4. 版本对比验证优化效果")
    print("5. 自动化测试提升开发效率")
    print("="*60)


if __name__ == "__main__":
    main()
```

### 运行演示

```bash
python prompt_testing_system.py
```

---

## 🎯 最佳实践总结

### 1. 测试集建设

```
✅ 规模：至少30-50个，推荐100+
✅ 质量：精心标注，避免错误
✅ 覆盖：简单、中等、困难全覆盖
✅ 更新：定期添加新的失败案例
✅ 分层：训练集、验证集、测试集
```

### 2. 评估指标选择

```
分类任务：
- 简单：准确率
- 推荐：F1分数
- 类别不平衡：Precision + Recall

生成任务：
- 主观评分（1-5分）
- ROUGE/BLEU（如适用）
- 用户满意度

关键：选择与业务目标对齐的指标
```

### 3. 优化迭代策略

```
1. 建立基准
2. 分析错误
3. 针对性改进（一次一处）
4. 验证效果
5. 记录结果
6. 持续迭代

目标：
- 不要追求100%准确率（成本太高）
- 设定合理目标（如85-90%）
- 关注最重要的错误类型
```

---

## ✅ 课后检验

完成本课后，你应该能够：

- [ ] 构建提示词测试数据集
- [ ] 使用多种指标评估效果
- [ ] 进行系统化的错误分析
- [ ] 设计和执行A/B测试
- [ ] 建立自动化测试流程

---

## 📝 下一课预告

**第14课：提示词版本管理与团队协作**

个人开发有套路，团队协作更需要规范！下一课我们将学习：
- 提示词的版本控制
- 团队协作的最佳实践
- 提示词文档化
- 知识库的建设

**让提示词工程更加规范和可维护！**

---

**🎉 恭喜你完成第13课！**

你已经掌握了科学的测试和评估方法！

**下一步：** 打开 `第14课-提示词版本管理.md`

