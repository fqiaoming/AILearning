![Tool Calling流程](./images/tool_calling.svg)
*图：Tool Calling流程*

# 第78课：内置工具使用

> **本课目标**：掌握常用内置工具的使用和集成
> 
> **核心技能**：Calculator、Search、DateTime、WebBrowser、Python REPL
> 
> **实战案例**：构建多功能工具集Agent
> 
> **学习时长**：75分钟

---

## 📖 口播文案（5分钟）
![Tool Chain](./images/tool_chain.svg)
*图：Tool Chain*


### 🎯 前言

"前面两节课我们学习了Tool Calling和Function Calling的原理。

今天我们要学习：**如何使用现成的内置工具！**

**为什么要用内置工具？**

因为：**不需要重新造轮子！**

**常用的5大内置工具：**

**1. Calculator（计算器）**
```
功能：数学计算
场景：
• 复杂数学运算
• 单位转换
• 财务计算

示例：
"(123 + 456) * 0.85 - 234 / 5.6"
→ 结果：450.43
```

**2. Search（搜索引擎）**
```
功能：网络搜索
场景：
• 实时信息查询
• 新闻搜索
• 知识检索

示例：
"Python 3.12新特性"
→ 返回最新搜索结果
```

**3. DateTime（日期时间）**
```
功能：时间处理
场景：
• 获取当前时间
• 日期计算
• 时区转换

示例：
"3天后是几号？"
→ 2024-11-18
```

**4. WebBrowser（网页浏览）**
```
功能：访问网页
场景：
• 抓取网页内容
• 获取实时数据
• 验证链接

示例：
"访问github.com"
→ 返回页面内容
```

**5. Python REPL（代码执行）**
```
功能：执行Python代码
场景：
• 数据处理
• 复杂计算
• 快速验证

示例：
"生成1-100的质数"
→ [2, 3, 5, 7, 11, ...]
```

**真实案例对比：**

**场景：数据分析任务**

```
用户："我有一个Excel文件，
      帮我算一下平均值和标准差"

❌ 没有工具：
"抱歉，我无法处理文件..."

✅ 有Python REPL：
1. 读取Excel
2. 计算统计值
3. 返回结果

完美解决！
```

**LangChain的工具生态：**

```python
# LangChain提供了丰富的工具
from langchain.agents import load_tools

# 加载工具
tools = load_tools([
    "llm-math",        # 数学计算
    "serpapi",         # 搜索
    "requests",        # HTTP请求
    "terminal",        # 终端命令
    "python_repl",     # Python解释器
])

# 轻松集成！
```

**今天这一课，我要带你：**

**第一部分：Calculator工具**
- 基础计算
- 复杂表达式
- 单位转换

**第二部分：Search工具**
- SerpAPI集成
- 搜索结果解析
- 实时信息

**第三部分：DateTime工具**
- 时间获取
- 日期计算
- 时区处理

**第四部分：Python REPL**
- 代码执行
- 数据处理
- 安全控制

**第五部分：工具集成**
- 多工具Agent
- 工具选择
- 实战案例

学完这一课，你将拥有强大的工具库！

准备好了吗？让我们开始！"

---

### 💡 核心理念

```
【站在巨人的肩膀上】

自己实现：
• 费时费力
• 容易有bug
• 功能有限

使用内置工具：
• 开箱即用
• 经过验证
• 功能强大

【工具组合的威力】

单个工具：有限
工具组合：无限可能！
```

---

## 📚 第一部分：Calculator工具

### 一、基础Calculator实现

```python
import re
import math
from typing import Dict, Any

class CalculatorTool:
    """计算器工具"""
    
    def __init__(self):
        self.name = "calculator"
        self.description = "执行数学计算。支持基本运算、三角函数、对数等。输入数学表达式，如'2+3*4'或'sin(pi/2)'"
        
        # 安全的数学函数
        self.safe_functions = {
            'sin': math.sin,
            'cos': math.cos,
            'tan': math.tan,
            'sqrt': math.sqrt,
            'log': math.log,
            'log10': math.log10,
            'exp': math.exp,
            'abs': abs,
            'pow': pow,
            'pi': math.pi,
            'e': math.e
        }
    
    def run(self, expression: str) -> str:
        """执行计算"""
        try:
            # 清理表达式
            expression = expression.strip()
            
            # 替换常量
            expression = expression.replace('π', 'pi')
            expression = expression.replace('^', '**')
            
            # 创建安全的执行环境
            safe_dict = {"__builtins__": {}}
            safe_dict.update(self.safe_functions)
            
            # 计算
            result = eval(expression, safe_dict)
            
            return f"计算结果: {result}"
            
        except ZeroDivisionError:
            return "错误：除数不能为零"
        except Exception as e:
            return f"计算错误：{str(e)}"
    
    def to_dict(self) -> Dict:
        """转为工具定义"""
        return {
            "type": "function",
            "function": {
                "name": self.name,
                "description": self.description,
                "parameters": {
                    "type": "object",
                    "properties": {
                        "expression": {
                            "type": "string",
                            "description": "要计算的数学表达式"
                        }
                    },
                    "required": ["expression"]
                }
            }
        }

# 演示
def demo_calculator():
    """演示计算器"""
    
    calc = CalculatorTool()
    
    print("="*60)
    print("Calculator工具演示")
    print("="*60)
    
    test_expressions = [
        "2 + 3 * 4",
        "(100 - 32) * 5/9",  # 华氏度转摄氏度
        "sqrt(144)",
        "sin(pi/2)",
        "log(100, 10)",
        "2 ** 10"
    ]
    
    for expr in test_expressions:
        print(f"\n表达式: {expr}")
        result = calc.run(expr)
        print(f"  {result}")

demo_calculator()
```

---

## 💻 第二部分：Search工具

### 一、搜索工具实现

```python
class SearchTool:
    """搜索工具（模拟实现）"""
    
    def __init__(self, api_key: str = None):
        self.name = "search"
        self.description = "在网络上搜索最新信息。输入搜索关键词。"
        self.api_key = api_key
    
    def run(self, query: str, num_results: int = 3) -> str:
        """执行搜索"""
        
        # 实际应用中这里会调用真实的搜索API
        # 如：SerpAPI、Google Custom Search、Bing Search等
        
        # 模拟搜索结果
        results = f"""搜索关键词：{query}

搜索结果：

1. 【标题】{query}的完整指南
   来源：example.com
   摘要：这是关于{query}的详细介绍...

2. 【标题】{query}最佳实践
   来源：tutorial.com
   摘要：学习{query}的最佳方法和技巧...

3. 【标题】{query}常见问题解答
   来源：faq.com
   摘要：关于{query}的常见问题和解答...
"""
        
        return results
    
    def to_dict(self) -> Dict:
        """转为工具定义"""
        return {
            "type": "function",
            "function": {
                "name": self.name,
                "description": self.description,
                "parameters": {
                    "type": "object",
                    "properties": {
                        "query": {
                            "type": "string",
                            "description": "搜索关键词"
                        },
                        "num_results": {
                            "type": "integer",
                            "description": "返回结果数量，默认3"
                        }
                    },
                    "required": ["query"]
                }
            }
        }

# 真实的SerpAPI集成示例
class SerpAPISearchTool:
    """SerpAPI搜索工具"""
    
    def __init__(self, api_key: str):
        self.name = "serpapi_search"
        self.description = "使用Google搜索最新信息"
        self.api_key = api_key
    
    def run(self, query: str) -> str:
        """执行搜索"""
        try:
            # 实际使用时需要安装: pip install google-search-results
            # from serpapi import GoogleSearch
            
            # params = {
            #     "q": query,
            #     "api_key": self.api_key,
            #     "num": 3
            # }
            
            # search = GoogleSearch(params)
            # results = search.get_dict()
            
            # 处理结果
            # ...
            
            return f"搜索'{query}'的结果..."
            
        except Exception as e:
            return f"搜索失败：{str(e)}"
```

---

## 🎯 第三部分：DateTime工具

### 一、日期时间工具

```python
from datetime import datetime, timedelta
import pytz

class DateTimeTool:
    """日期时间工具"""
    
    def __init__(self):
        self.name = "datetime"
        self.description = "处理日期时间相关操作。支持：获取当前时间、日期计算、时区转换"
    
    def run(self, action: str, **kwargs) -> str:
        """执行操作"""
        
        if action == "now":
            return self._get_now(kwargs.get('timezone', 'Asia/Shanghai'))
        elif action == "add_days":
            return self._add_days(kwargs.get('days', 0))
        elif action == "format":
            return self._format_date(kwargs.get('format', '%Y-%m-%d %H:%M:%S'))
        elif action == "diff":
            return self._date_diff(kwargs.get('date1'), kwargs.get('date2'))
        else:
            return "不支持的操作"
    
    def _get_now(self, timezone: str) -> str:
        """获取当前时间"""
        try:
            tz = pytz.timezone(timezone)
            now = datetime.now(tz)
            return f"当前时间：{now.strftime('%Y-%m-%d %H:%M:%S %Z')}"
        except:
            now = datetime.now()
            return f"当前时间：{now.strftime('%Y-%m-%d %H:%M:%S')}"
    
    def _add_days(self, days: int) -> str:
        """日期加减"""
        target_date = datetime.now() + timedelta(days=days)
        return f"{days}天后是：{target_date.strftime('%Y-%m-%d')}"
    
    def _format_date(self, format_str: str) -> str:
        """格式化日期"""
        now = datetime.now()
        return now.strftime(format_str)
    
    def _date_diff(self, date1_str: str, date2_str: str) -> str:
        """计算日期差"""
        try:
            date1 = datetime.strptime(date1_str, '%Y-%m-%d')
            date2 = datetime.strptime(date2_str, '%Y-%m-%d')
            diff = (date2 - date1).days
            return f"相差{abs(diff)}天"
        except:
            return "日期格式错误，请使用YYYY-MM-DD"
    
    def to_dict(self) -> Dict:
        """转为工具定义"""
        return {
            "type": "function",
            "function": {
                "name": self.name,
                "description": self.description,
                "parameters": {
                    "type": "object",
                    "properties": {
                        "action": {
                            "type": "string",
                            "enum": ["now", "add_days", "format", "diff"],
                            "description": "操作类型"
                        },
                        "days": {
                            "type": "integer",
                            "description": "天数（用于add_days）"
                        },
                        "timezone": {
                            "type": "string",
                            "description": "时区（用于now）"
                        },
                        "format": {
                            "type": "string",
                            "description": "日期格式（用于format）"
                        },
                        "date1": {
                            "type": "string",
                            "description": "日期1（用于diff）"
                        },
                        "date2": {
                            "type": "string",
                            "description": "日期2（用于diff）"
                        }
                    },
                    "required": ["action"]
                }
            }
        }

# 演示
def demo_datetime():
    """演示日期时间工具"""
    
    dt_tool = DateTimeTool()
    
    print("="*60)
    print("DateTime工具演示")
    print("="*60)
    
    # 测试各种操作
    print("\n1. 获取当前时间:")
    print(dt_tool.run("now"))
    
    print("\n2. 计算3天后:")
    print(dt_tool.run("add_days", days=3))
    
    print("\n3. 计算7天前:")
    print(dt_tool.run("add_days", days=-7))
    
    print("\n4. 格式化日期:")
    print(dt_tool.run("format", format="%Y年%m月%d日"))

demo_datetime()
```

---

## ⚡ 第四部分：Python REPL工具

### 一、安全的Python执行器

```python
import sys
from io import StringIO
from contextlib import redirect_stdout

class PythonREPLTool:
    """Python REPL工具（安全版）"""
    
    def __init__(self):
        self.name = "python_repl"
        self.description = "执行Python代码。可以进行数据处理、计算等操作。输入Python代码。"
        
        # 危险的内置函数（禁止使用）
        self.forbidden = [
            'exec', 'eval', 'compile', '__import__',
            'open', 'file', 'input', 'raw_input'
        ]
    
    def run(self, code: str) -> str:
        """执行Python代码"""
        
        # 安全检查
        for forbidden in self.forbidden:
            if forbidden in code:
                return f"错误：禁止使用{forbidden}"
        
        try:
            # 捕获输出
            output = StringIO()
            
            with redirect_stdout(output):
                # 在受限环境中执行
                local_vars = {}
                exec(code, {"__builtins__": __builtins__}, local_vars)
                
                # 如果有返回值，打印它
                if local_vars:
                    for key, value in local_vars.items():
                        if not key.startswith('_'):
                            print(f"{key} = {value}")
            
            result = output.getvalue()
            return result if result else "代码执行完成（无输出）"
            
        except Exception as e:
            return f"执行错误：{str(e)}"
    
    def to_dict(self) -> Dict:
        """转为工具定义"""
        return {
            "type": "function",
            "function": {
                "name": self.name,
                "description": self.description,
                "parameters": {
                    "type": "object",
                    "properties": {
                        "code": {
                            "type": "string",
                            "description": "要执行的Python代码"
                        }
                    },
                    "required": ["code"]
                }
            }
        }

# 演示
def demo_python_repl():
    """演示Python REPL"""
    
    repl = PythonREPLTool()
    
    print("="*60)
    print("Python REPL工具演示")
    print("="*60)
    
    test_codes = [
        "print('Hello, World!')",
        "result = sum(range(1, 101))\nprint(f'1到100的和：{result}')",
        "import math\nprint(f'圆周率：{math.pi}')",
        "[x**2 for x in range(10)]"
    ]
    
    for i, code in enumerate(test_codes, 1):
        print(f"\n{i}. 执行代码:")
        print(f"```python\n{code}\n```")
        print("输出:")
        result = repl.run(code)
        print(result)

demo_python_repl()
```

---

## 📝 课后练习

### 练习1：扩展Calculator
添加更多数学函数和单位转换

### 练习2：集成真实Search API
使用SerpAPI或其他搜索API

### 练习3：文件工具
实现安全的文件读写工具

---

## 🎓 知识总结

### 核心要点

1. **5大内置工具**
   - Calculator：数学计算
   - Search：网络搜索
   - DateTime：时间处理
   - WebBrowser：网页访问
   - Python REPL：代码执行

2. **工具设计原则**
   - 功能单一明确
   - 参数清晰简单
   - 错误处理完善
   - 安全性第一

3. **工具集成**
   - 统一接口
   - 易于扩展
   - 组合使用

4. **安全考虑**
   - 输入验证
   - 权限控制
   - 资源限制

---

## 🚀 下节预告

下一课：**第79课：自定义工具开发-API调用**

- HTTP请求封装
- API认证
- 错误处理
- 实战案例

**开发自己的API工具！** 🔧

---

**💪 记住：善用工具，事半功倍！**

**下一课见！** 🎉
